Clase Usuario
	id
	name
	username
	email

la arquitectura va de cliente---->web ---->gateway y de aqui va a tres instancias
	App01
	App02
	AppUsuarios---->DBUsuarios

App01: Servicio Rets que lista Post asociados a publicaciones, el servicio esta en este link (solo consumir, no programar)
	https://jsonplaceholder.typicode.com/posts

App02: Servicio Rets que lista Comments asociados a publicaciones, el servicio esta en este link (solo consumir, no programar)
	https://jsonplaceholder.typicode.com/comments

AppUsuarios: aplicacion REST que lista usuarios representados por la clase Usuarios. Un post esta asociado a un usuario.

Web: permite listar Post y Comments asociados a un post particular (con JSF2 igual q el examen)

Gateway: configurado para q la aplicacion web solo se comunique a través de gateway.
